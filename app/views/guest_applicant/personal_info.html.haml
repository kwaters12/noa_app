= simple_form_for @client, url: wizard_path, method: :patch do |f|
  .col-md-6.col-md-offset-3
    .text-center
      %hr
      %h3 Guest Applicant
      %hr
      = f.input :first_name, required: true
      = f.input :last_name, required: true
      = f.input :email, required: true
      = f.input :phone_num
    %p Are you a mortgage broker? Please provide your email, so we may share the documents with you upon completion.
    .text-center
      = f.input :broker_id
      = f.hidden_field :client_id unless @client.nil?
    = f.submit "Continue", class: "btn btn-default"